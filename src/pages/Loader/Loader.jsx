import React from 'react';

const Loader = () => {
    return (
        <div>
            <span class="loading loading-spinner loading-xl text-success"></span>
        </div>
    );
};

export default Loader;